# dependencias del backend FamilyMed

# framework
Django>=5.0
djangorestframework>=3.15

# tema del admin
django-jazzmin>=3.0

# conexion a MongoDB Atlas
pymongo>=4.6
certifi>=2024.0

# variables de entorno desde .env
python-dotenv>=1.0

# autenticacion: hash de passwords + JWT propio
bcrypt>=4.0
PyJWT>=2.8

# produccion: servidor wsgi, archivos estaticos y cors
gunicorn>=22.0
whitenoise>=6.7
django-cors-headers>=4.4
