# plantilla de variables de entorno de la app movil (NO subir el .env real a git)
# copialo como .env en la raiz y llena el valor antes de hacer eas build

# url completa del backend en produccion (sin slash final, sin /api)
EXPO_PUBLIC_API_URL=https://familymed-backend.onrender.com
